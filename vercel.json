{
  "version": 2,
  "installCommand": "uv pip install -r requirements.txt --system --only-binary=psycopg2-binary",
  "buildCommand": "python manage.py collectstatic --noinput --clear",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api/wsgi"
    }
  ]
}
